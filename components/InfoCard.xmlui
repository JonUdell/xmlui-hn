<Component name="InfoCard">

    <DataSource id="data" url="sqlite:memory" method="POST" dataType="sql" body='{ $props.sql }' />

    <Card title="{$props.title}">
        <Items data="{data}">
            <Text>{$item.value}</Text>
        </Items>
    </Card>

</Component>