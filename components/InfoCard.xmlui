<Component name="InfoCard">

    <DataSource id="data" url="{ window.xmluiHost + '/query' }" body='{ $props.sql }' method="POST" />

    <Card title="{$props.title}">
        <Items data="{data}">
            <Text>{$item.value}</Text>
        </Items>
    </Card>

</Component>